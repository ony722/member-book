# リスク管理表（Risk Register）

| ID | リスク | 影響度 | 発生確率 | 対策案 | 残存リスク | 関連要件 |
|---|---|---|---|---|---|---|
| RISK-001 | `page-flip`ライブラリが約5年間更新されておらず、将来のブラウザ仕様変更で動作しなくなる可能性。**Phase 6実装時に実際に顕在化**: `page-flip@2.0.7`の実行時バンドル（`dist/js/page-flip.browser.js`）は`PageFlip`クラスのみをエクスポートし、`@types/page-flip`が型定義する`SizeType`列挙型を実行時値としてエクスポートしていなかった（型定義とビルド成果物の不一致というライブラリ側の潜在バグ）。`SizeType.STRETCH`をそのまま使うと実行時に`TypeError`が発生し、意図せずフォールバックUIに切り替わっていた | 中 | 中（実測: 1回発生） | 実地検証済み（EV-BUILD-PAGEFLIP-001）。バージョンをpin固定し、CIビルド成功を都度の健全性確認とする。顕在化した`SizeType`未エクスポート問題は、文字列リテラル`'stretch'`を型キャストして使う方式で回避済み（`src/ui/flipBookView.ts`参照、修正確認はE2E-01〜06全PASSで実施）。破損時はCSS 3D Transform代替に切替（adoption_stop_criteria.md参照） | 低（回避策を実装しE2E全PASSで確認済み。ただし同種の型定義とビルド成果物の乖離が他のAPIでも起きうるため、新機能追加時は都度実挙動を確認する運用とする） | FR-SYS-004 |
| RISK-002 | スマホでめくりアニメーションが重くなる（操作性低下） | 中 | 中 | HTMLモード（軽量）を既定にし、NFR-PERF-001/002で数値目標を設定。実機相当ビューポートでのE2Eを必須TCとする | 中（実機検証はE2E自動化の範囲に限定されるため、Phase7で目視確認を推奨） | FR-SYS-001, 004, 010, NFR-PERF-001 |
| RISK-003 | データ追加のたびにコード改修が必要になり運用負荷が上がる。**Round1レビュー指摘F-O3で判明**: 実際の更新手順（git clone→ブランチ作成→JSON編集→コミット→PR作成→レビュー→マージ）は非エンジニアが単独で完結するには難度が高く、「JSON編集のみで運用可能」という当初評価は開発者による更新を前提にした場合に限られる | 低 | 高 | 会員データをJSON分離しコード非改修でのデータ追加を可能にする設計（FR-DATA-001）。管理画面はDEFER。非エンジニア向けにはGitHub Web UI（ブラウザ上でのファイル直接編集機能）での更新手順を簡易ガイドとして別途用意する（SRS第4章に追記） | **中**（Round1レビューでJSON編集自体は容易でもGit/PR運用フローは非エンジニアには高難度と判明したため、初版の「低」から格上げ） | FR-SYS-002, 003, 007, FR-DATA-001 |
| RISK-004 | 会員ページの外部リンクが不正なURL・危険なドメインを指してしまう | 低 | 低 | ビルド時にURLバリデーション（http/https必須）を実施。`noopener noreferrer`でタブナビゲーション経由の脆弱性を防止（NFR-SEC-001） | 低 | FR-EXT-001, NFR-SEC-001 |
| RISK-005 | GitHub Pagesの無料枠がpublicリポジトリ前提のため会員データが誰でも閲覧可能になる | 低 | 中 | Phase 0で「誰でも見られる公開サイト」と確定済みのため許容。実データ投入前に公開可否を再確認（Open Item #4、intake_sheet.md） | 低（要件と整合） | 第11章（SRS）, FR-SYS-002 |
| RISK-006 | 実データ投入後、会員本人から掲載中止・削除を求められた際の対応プロセスが未整備（Round1レビュー指摘F-O2） | 中 | 中 | (1) 実データ投入前に本人同意の取得方法を確定する（Phase 8着手条件とする）。(2) 削除依頼受領時はJSON削除＋リビルドに加え、Git履歴・検索エンジンインデックスへの対応をCONSTRAINTS.md第7章「機微情報混入時のインシデント対応手順」に準じて実施する。(3) CLAUDE.md PART G（本番移行チェックリスト）に同意取得方法の確認を追加済み | 中（プロセスは設計したが実運用での検証は未実施） | SRS第12章, CLAUDE.md PART G |
| RISK-007 | 認証なし公開サイトのため会員の実名・リンクが検索エンジンにインデックスされ、事後の完全な削除が困難になる可能性（Round1レビュー指摘F-O4） | 低 | 中 | 実データ投入時に`robots.txt`/`<meta name="robots" content="noindex">`でのインデックス抑制要否を判断し、判断結果と理由をSDD.mdに記録する（現時点ではサンプルデータのみのため未着手、Phase 8着手条件とする） | 中（対応方針を今後確定する必要がある） | SRS第11章, RISK-005 |

**リスク台帳の完全性**: 全リスクが要件IDと双方向接続済み。孤立リスクなし。RISK-006/007はRound1レビュー（2026-07-23）で追加。
